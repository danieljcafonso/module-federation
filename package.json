{
  "name": "module-federation",
  "version": "0.0.0",
  "description": "Trying Webpack 5 Module Federation",
  "private": true,
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "workspaces": [
    "*"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build",
    "serve": "lerna run --parallel serve"
  }
}
